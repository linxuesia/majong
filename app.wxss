/* app.wxss - 全局样式 */

/* 全局常量定义 */
/* 主色调 */
/* --primary-color: #D32F2F; */
/* --primary-light: #F44336; */
/* --primary-dark: #B71C1C; */

/* 辅助色 */
/* --secondary-color: #FFB74D; */
/* --secondary-light: #FFCC80; */
/* --secondary-dark: #F57C00; */

/* 成功色 */
/* --success-color: #4CAF50; */
/* --success-light: #8BC34A; */
/* --success-dark: #388E3C; */

/* 背景色 */
/* --bg-primary: #FFF8E1; */
/* --bg-secondary: #FFECB3; */
/* --bg-card: #FFFFFF; */

/* 文本色 */
/* --text-primary: #333333; */
/* --text-secondary: #666666; */
/* --text-light: #999999; */

/* 边框和阴影 */
/* --border-radius: 24rpx; */
/* --box-shadow-sm: 0 4rpx 12rpx rgba(0, 0, 0, 0.1); */
/* --box-shadow-md: 0 8rpx 24rpx rgba(0, 0, 0, 0.15); */
/* --box-shadow-lg: 0 16rpx 48rpx rgba(0, 0, 0, 0.2); */
/* --box-shadow-primary: 0 8rpx 16rpx rgba(211, 47, 47, 0.3); */

/* 动画时长 */
/* --anim-fast: 0.2s; */
/* --anim-normal: 0.3s; */
/* --anim-slow: 0.5s; */

page {
  background: linear-gradient(135deg, #FFF8E1 0%, #FFECB3 100%);
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', sans-serif;
  min-height: 100vh;
}

.container {
  box-sizing: border-box;
}

/* 通用按钮样式 */
.option-btn {
  box-sizing: border-box;
  width: 130px !important;
}

.btn-primary {
  background: linear-gradient(135deg, #D32F2F 0%, #F44336 100%);
  color: white;
  border-radius: 50rpx;
  padding: 24rpx 48rpx;
  font-size: 32rpx;
  font-weight: bold;
  box-shadow: 0 8rpx 16rpx rgba(211, 47, 47, 0.3);
  border: none;
  position: relative;
  overflow: hidden;
  transition: all 0.3s ease;
}

.btn-primary::before {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  width: 0;
  height: 0;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.3);
  transform: translate(-50%, -50%);
  transition: width 0.6s, height 0.6s;
}

.btn-primary:active::before {
  width: 300rpx;
  height: 300rpx;
}

.btn-primary:active {
  opacity: 0.9;
  transform: scale(0.97);
  box-shadow: 0 4rpx 8rpx rgba(211, 47, 47, 0.4);
}

.btn-primary:hover {
  transform: translateY(-2rpx);
  box-shadow: 0 12rpx 24rpx rgba(211, 47, 47, 0.4);
}

.btn-secondary {
  background: #FFFFFF;
  color: #D32F2F;
  border: 4rpx solid #D32F2F;
  border-radius: 50rpx;
  padding: 24rpx 48rpx;
  font-size: 32rpx;
  font-weight: bold;
  transition: all 0.3s ease;
  position: relative;
  overflow: hidden;
}

.btn-secondary::before {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  width: 0;
  height: 0;
  border-radius: 50%;
  background: rgba(211, 47, 47, 0.1);
  transform: translate(-50%, -50%);
  transition: width 0.6s, height 0.6s;
}

.btn-secondary:active::before {
  width: 300rpx;
  height: 300rpx;
}

.btn-secondary:active {
  transform: scale(0.97);
  background: rgba(211, 47, 47, 0.1);
}

.btn-secondary:hover {
  transform: translateY(-2rpx);
  box-shadow: 0 8rpx 24rpx rgba(0, 0, 0, 0.15);
}

/* 卡片样式 */
.card {
  background: #FFFFFF;
  border-radius: 24rpx;
  padding: 32rpx;
  margin: 24rpx;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1);
  transition: all 0.3s ease;
  position: relative;
  overflow: hidden;
}

.card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4rpx;
  background: linear-gradient(90deg, #D32F2F, #FFB74D, #4CAF50);
}

.card:hover {
  transform: translateY(-4rpx);
  box-shadow: 0 8rpx 24rpx rgba(0, 0, 0, 0.15);
}

/* 麻将牌样式 */
.mahjong-tile {
  width: 66rpx;
  height: 97rpx;
  background: #FFFFFF;
  border: 2rpx solid #D32F2F;
  border-radius: 12rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 48rpx;
  box-shadow: 2rpx 2rpx 8rpx rgba(0, 0, 0, 0.2);
  transition: all 0.2s ease;
  position: relative;
  overflow: hidden;
}

.mahjong-tile::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 2rpx;
  background: linear-gradient(90deg, #D32F2F, #FFB74D);
  opacity: 0;
  transition: opacity 0.2s ease;
}

.mahjong-tile:hover {
  transform: translateY(-4rpx) scale(1.05);
  box-shadow: 4rpx 8rpx 16rpx rgba(0, 0, 0, 0.3);
}

.mahjong-tile:active {
  transform: translateY(-2rpx) scale(0.98);
}

.mahjong-tile.selected::before {
  opacity: 1;
}

.mahjong-tile.selected {
  border-color: #FFB74D;
  box-shadow: 0 0 20rpx rgba(255, 183, 77, 0.6);
  animation: pulse 1s infinite;
}

/* 麻将牌移除动画 */
.mahjong-tile.removed {
  opacity: 0;
  transform: translateX(-100vw) translateY(-100vh) rotate(360deg);
  pointer-events: none;
  transition: all 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  position: relative;
  z-index: 10;
}

/* 麻将牌图片样式 */
.tile-image {
  width: 70rpx;
  height: 90rpx;
  border-radius: 10rpx;
  transition: all 0.2s ease;
}

.tile-image:hover {
  transform: scale(1.1);
}

/* 动画效果 */

/* 弹跳效果 */
@keyframes bounce {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-20rpx); }
}

.bounce {
  animation: bounce 0.6s ease-in-out;
}

/* 摇晃效果 */
@keyframes shake {
  0%, 100% { transform: translateX(0); }
  25% { transform: translateX(-10rpx); }
  75% { transform: translateX(10rpx); }
}

.shake {
  animation: shake 0.4s ease-in-out;
}

/* 渐入效果 */
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

.fadeIn {
  animation: fadeIn 0.3s ease-out;
}

/* 滑入效果 */
@keyframes slideIn {
  from { transform: translateY(50rpx); opacity: 0; }
  to { transform: translateY(0); opacity: 1; }
}

.slideIn {
  animation: slideIn 0.3s ease-out;
}

/* 缩放进入效果 */
@keyframes scaleIn {
  from { transform: scale(0.8); opacity: 0; }
  to { transform: scale(1); opacity: 1; }
}

.scaleIn {
  animation: scaleIn 0.3s ease-out;
}

/* 旋转进入效果 */
@keyframes rotateIn {
  from { transform: rotate(-180deg) scale(0.8); opacity: 0; }
  to { transform: rotate(0deg) scale(1); opacity: 1; }
}

.rotateIn {
  animation: rotateIn 0.5s ease-out;
}

/* 脉冲效果 */
@keyframes pulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.05); }
}

.pulse {
  animation: pulse 1s infinite;
}

/* 波纹效果 */
@keyframes ripple {
  0% {
    transform: scale(0);
    opacity: 0.8;
  }
  100% {
    transform: scale(2);
    opacity: 0;
  }
}

.ripple {
  position: relative;
  overflow: hidden;
}

.ripple::after {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  width: 0;
  height: 0;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.5);
  transform: translate(-50%, -50%);
  animation: ripple 0.6s ease-out;
  pointer-events: none;
}

/* 闪光效果 */
@keyframes shine {
  0% {
    background-position: -200% center;
  }
  100% {
    background-position: 200% center;
  }
}

.shine {
  position: relative;
  overflow: hidden;
}

.shine::before {
  content: '';
  position: absolute;
  top: 0;
  left: -200%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
  animation: shine 1.5s infinite;
}

/* 心跳效果 */
@keyframes heartbeat {
  0%, 100% {
    transform: scale(1);
  }
  14% {
    transform: scale(1.1);
  }
  28% {
    transform: scale(1);
  }
  42% {
    transform: scale(1.1);
  }
  70% {
    transform: scale(1);
  }
}

.heartbeat {
  animation: heartbeat 1.5s infinite;
}

/* 浮动效果 */
@keyframes float {
  0%, 100% {
    transform: translateY(0);
  }
  50% {
    transform: translateY(-10rpx);
  }
}

.float {
  animation: float 3s ease-in-out infinite;
}

/* 摇摆效果 */
@keyframes swing {
  0%, 100% {
    transform: rotate(0deg);
  }
  25% {
    transform: rotate(5deg);
  }
  75% {
    transform: rotate(-5deg);
  }
}

.swing {
  animation: swing 2s ease-in-out infinite;
}
